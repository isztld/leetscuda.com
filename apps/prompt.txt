Implementation prompts for judge-cuda-cpp and judge-k8s.
Use one session per block. Read the relevant ARCHITECTURE.md before starting each session.

════════════════════════════════════════════════════════════
SESSION 1 — judge-cuda-cpp scaffold + types + env
════════════════════════════════════════════════════════════

Read apps/judge-cuda-cpp/ARCHITECTURE.md, then implement the scaffold for
judge-cuda-cpp. Do not implement any logic yet — only the project skeleton.

Create:
- package.json  (name: judge-cuda-cpp, type: module, tsx dev dep, zod dep,
                 scripts: dev, build, start)
- tsconfig.json (ESNext, NodeNext modules, strict, outDir: dist)
- .env.example  (all variables from the env table in the ARCHITECTURE, with
                 comments explaining each one)
- .gitignore    (node_modules, dist, .env, tmp)
- src/types.ts  (all zod schemas and TypeScript types: TestCaseSchema,
                 CppJudgeJobSchema, JudgeResultSchema, SubmissionTestResult —
                 match the shapes used in the current apps/judge/src/types.ts
                 but model them cleanly from scratch)
- src/env.ts    (zod env schema, parseCapabilities(), startup validation:
                 seccomp profile check, root-process warning, DOCKER_HOST
                 warning, CUDA capability log + nvidia-smi verify — all from
                 §9 of the ARCHITECTURE)

════════════════════════════════════════════════════════════
SESSION 2 — judge-cuda-cpp api-client + verifier
════════════════════════════════════════════════════════════

Read apps/judge-cuda-cpp/ARCHITECTURE.md §4 and §8. Then implement:

- src/api-client.ts
    pollForJob()               — GET /api/judge/poll, zod validate response,
                                 exponential backoff on network errors (5s→60s),
                                 exit on 401, sleep 60s on 429
    submitResult()             — POST /api/judge/result, one automatic retry on
                                 network error, exit on 401
    recoverStuckSubmissions()  — POST /api/judge/recover

- src/verifier.ts
    verify(actual, expected)   — literal match, float tolerance (~N value),
                                 fill shorthand expansion (fill(N, value)),
                                 multi-line matching; pure function, no I/O

Carry over the logic from apps/judge/src/api-client.ts and
apps/judge/src/verifier.ts but rewrite them cleanly against the new types.

════════════════════════════════════════════════════════════
SESSION 3 — judge-cuda-cpp sandbox
════════════════════════════════════════════════════════════

Read apps/judge-cuda-cpp/ARCHITECTURE.md §6 and §7 carefully — this is the
core of the judge.

Implement the full sandbox module:

- src/sandbox/sanitize.ts
    sanitizeOutput(s)   — strip ANSI CSI + ESC sequences, keep tab/LF/CR/
                          printable ASCII only

- src/sandbox/proxy.ts
    verifyDockerProxy()             — docker ps via proxy, exit on failure
    cleanupOrphanedContainers()     — docker ps filter leetscuda-judge=1,
                                      docker stop --time 1 each

- src/sandbox/build.ts
    runBuildContainer(code, opts)   — Phase 1: create → cp source → start →
                                      wait (30s timeout) → cp binary out →
                                      rm -f; return { binary: Buffer, stderr,
                                      exitCode }
                                      All docker flags from §7.2

- src/sandbox/eval.ts
    runEvalContainer(binary, input, opts)  — Phase 2: create → cp binary →
                                             cp input → start → race(wait,
                                             sleep(timeout)) → two-phase kill
                                             if timeout → logs → rm -f
                                             All docker flags from §7.3

- src/sandbox/index.ts
    runInSandbox(code, input, opts) — orchestrates build then eval; falls
                                      back to runDirect() if docker unavailable
                                      (dev only — log prominent WARNING);
                                      sanitizes stdout/stderr before returning

════════════════════════════════════════════════════════════
SESSION 4 — judge-cuda-cpp main loop + health server
════════════════════════════════════════════════════════════

Read apps/judge-cuda-cpp/ARCHITECTURE.md §5, §9, §10, §12.

Implement:

- src/main.ts
    main()    — run all startup checks in order (§9), then poll loop:
                  pollForJob() → secondary code-size guard → for each
                  test case: runInSandbox() → verify() → assemble
                  JudgeResult → submitResult()
                  Early exit on TIME_LIMIT or RUNTIME_ERROR (skip remaining
                  test cases)
                  After each cuda job: log GPU memory warning
                  SIGINT/SIGTERM handlers for clean shutdown

    startHealthServer()   — tiny http.createServer on JUDGE_HEALTH_PORT;
                            GET / returns 200 { status:"ok", capabilities }
                            when ready, 503 before ready or after shutdown

════════════════════════════════════════════════════════════
SESSION 5 — judge-cuda-cpp seccomp + Dockerfiles + compose
════════════════════════════════════════════════════════════

Read apps/judge-cuda-cpp/ARCHITECTURE.md §7.4, §13, and the existing
apps/judge/seccomp-judge.json and apps/judge/seccomp-judge-cuda.json.

Create:

- seccomp/cpp.json    — copy from apps/judge/seccomp-judge.json as the base;
                        review every syscall group and add a comment block
                        explaining what program behaviour each group enables

- seccomp/cuda.json   — extend cpp.json with the additional syscalls required
                        by the NVIDIA userspace driver; document why each
                        addition is needed

- Dockerfile          — multi-stage: builder (tsc compile), runner (node +
                        docker CLI client only, no Docker daemon, no compose);
                        COPY seccomp profiles to /etc/judge/seccomp/;
                        runs as non-root uid 1001

- Dockerfile.cuda-sandbox — based on nvidia/cuda:{CUDA_IMAGE}-devel-...; adds
                             nobody user uid 65534 with a real homedir so glibc
                             TLS init works; no CMD (image is used via
                             docker create + docker cp, not docker run)

- docker-compose.yml  — socket-proxy sidecar (§13.1) + judge service;
                        bind-mount seccomp profiles read-only;
                        read_only: true on judge; tmpfs /tmp:size=512m;
                        network_mode: host

════════════════════════════════════════════════════════════
SESSION 6 — judge-k8s scaffold + types + env + api-client
════════════════════════════════════════════════════════════

Read apps/judge-k8s/ARCHITECTURE.md §1–§5, §11, §13.

Implement the full scaffold and communication layer for judge-k8s:

- package.json, tsconfig.json, .gitignore, .env.example
    (same structure as judge-cuda-cpp; name: judge-k8s; add js-yaml dep)

- src/types.ts
    TestCaseSchema, K8sCheckSchema (type: schema | assertion | kubectl-dry-run,
    assert, path, op, value), K8sJudgeJobSchema (code, k8sMultiDoc,
    k8sChecks, timeoutMs), JudgeResultSchema — clean from scratch

- src/env.ts
    zod env schema; startup checks: root warning, kubeconform availability
    check, kubeconform offline smoke test with a minimal ConfigMap (§11)

- src/api-client.ts
    pollForJob(), submitResult(), recoverStuckSubmissions()
    Identical structure to judge-cuda-cpp api-client; copy and adjust

════════════════════════════════════════════════════════════
SESSION 7 — judge-k8s validator
════════════════════════════════════════════════════════════

Read apps/judge-k8s/ARCHITECTURE.md §6, §7, §8, §9 carefully.

Implement the full validator module:

- src/validator/jsonpath.ts
    resolvePath(doc, pathStr)   — tokenizer supporting dot notation, array
                                  index, and bracket keys (keys with dots/
                                  slashes); pure function; unit-testable

- src/validator/operators.ts
    applyOp(op, actual, expected) — all 9 operators from §8; unknown op
                                    returns false

- src/validator/parser.ts
    parseManifest(yaml, opts)   — size guard (JUDGE_MAX_MANIFEST_BYTES before
                                  any parsing); yaml.DEFAULT_SAFE_SCHEMA;
                                  single-doc and multi-doc; filter null docs;
                                  returns docs[] or throws ParseError

- src/validator/schema.ts
    runSchemaCheck(docs, check) — kind/apiVersion presence check (§6.2)

- src/validator/assertion.ts
    runAssertionCheck(docs, check) — JSONPath resolve across docs + applyOp
                                     (§6.3)

- src/validator/kubeconform.ts
    runKubeconformCheck(yaml, check, timeoutMs) — write temp file with mode
                                                  0600 and unique name;
                                                  kubeconform -strict -summary
                                                  -output json
                                                  -schema-location default;
                                                  parse JSON output; delete
                                                  temp file in finally (§6.4)

- src/validator/index.ts
    validateK8sManifest(yaml, checks, opts)  — parse → run all checks in
                                               order (no short-circuit) →
                                               return K8sValidationResult[];
                                               max-checks guard before loop

════════════════════════════════════════════════════════════
SESSION 8 — judge-k8s main loop + health server + Dockerfile
════════════════════════════════════════════════════════════

Read apps/judge-k8s/ARCHITECTURE.md §5, §14, §15.

Implement:

- src/main.ts
    Startup checks → poll loop: pollForJob() → code-size + max-checks guards
    → validateK8sManifest() → assemble JudgeResult (checkId→input,
    description→expected, message→actual, runtimeMs=0) → submitResult()
    SIGINT/SIGTERM handlers; health server on JUDGE_HEALTH_PORT (default 8081)

- Dockerfile
    multi-stage; install kubeconform from GitHub release (pin version via
    ARG KUBECONFORM_VERSION); copy binary to /usr/local/bin; run as non-root
    uid 1001; read_only filesystem (only /tmp is writable via tmpfs)

- docker-compose.yml
    Single judge-k8s service; no socket-proxy; read_only: true;
    tmpfs /tmp:size=64m; env_file .env

════════════════════════════════════════════════════════════
FINAL SESSION — integration + delete apps/judge
════════════════════════════════════════════════════════════

Both judges are implemented and locally tested. This session:

1. Verify the web app's /api/judge/poll, /api/judge/result, /api/judge/recover
   endpoints still work correctly with the new judges. Run both judges locally
   against the dev web app and submit a cpp problem, a cuda problem (if GPU
   available), and a k8s problem end-to-end.

2. Update docs/judge-architecture.md to reference the two new judges instead
   of the monolithic apps/judge. Update any section that references file paths,
   capability strings, or deployment instructions.

3. Update any CI/CD configs, docker-compose files, or deployment scripts
   that reference apps/judge to point to apps/judge-cuda-cpp and
   apps/judge-k8s instead.

4. Delete apps/judge from disk:
       rm -rf apps/judge

5. Commit.
