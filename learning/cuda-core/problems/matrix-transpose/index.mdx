---
slug: matrix-transpose
title: Matrix Transpose
difficulty: medium
track: cuda-core
tags:
  - memory
  - coalescing
  - shared-memory
status: published
author: community
xp: 200
runtime: cuda
cpp_standard: "17"
cuda_version: "13.0"
compute_cap: "sm_120"
---

## Background

One of the most important performance considerations in CUDA is **memory coalescing**. When threads in a warp access global memory, the hardware can merge those accesses into a single transaction only if the addresses are contiguous. A naïve matrix transpose reads rows (coalesced) but writes columns (uncoalesced), cutting throughput by up to 32×.

The standard fix is to use **shared memory** as a staging buffer. Each block loads a tile from the input matrix into shared memory — reading contiguous rows, so accesses are coalesced. It then writes the *transposed* tile to the output matrix by reading columns from shared memory, which are only `__syncthreads()` away and do not touch slow global memory.

## What to Implement

Implement `transpose`, a tiled CUDA kernel that transposes an N×N matrix:

```
out[col][row] = in[row][col]
```

Use a `TILE × TILE` shared memory buffer. Each block handles one tile:
1. Load `in[row][col]` into `smem[ty][tx]` (coalesced read from global memory)
2. `__syncthreads()` to ensure the tile is fully loaded
3. Write `smem[tx][ty]` to `out[blockIdx.x*TILE+tx][blockIdx.y*TILE+ty]` (coalesced write)

To avoid **shared memory bank conflicts** when reading the transposed tile, declare the shared buffer with a padding column: `float smem[TILE][TILE + 1]`.

## Constraints

- `N` is a multiple of `TILE` (you may assume `TILE = 32`)
- `1 ≤ N ≤ 4096`
- The matrix is stored in **row-major** order; element `(r, c)` is at offset `r * N + c`
- Do not modify the host code in `solve()`

## Example

**Input:** 2×2 matrix `[[1, 2], [3, 4]]` (N=2)

**Output:** `[[1, 3], [2, 4]]`

Element `(r, c)` of the output equals element `(c, r)` of the input.

---starter-code---
#include <cuda_runtime.h>

#define TILE 32

// TODO: Implement the tiled matrix transpose kernel.
// Use shared memory to ensure coalesced global memory accesses.
__global__ void transpose(const float* in, float* out, int N) {
    __shared__ float smem[TILE][TILE + 1]; // +1 padding avoids bank conflicts

    // Your code here
}

// Do NOT modify below this line.
void solve(const float* h_in, float* h_out, int N) {
    float *d_in, *d_out;
    size_t bytes = (size_t)N * N * sizeof(float);
    cudaMalloc(&d_in,  bytes);
    cudaMalloc(&d_out, bytes);
    cudaMemcpy(d_in, h_in, bytes, cudaMemcpyHostToDevice);

    dim3 block(TILE, TILE);
    dim3 grid(N / TILE, N / TILE);
    transpose<<<grid, block>>>(d_in, d_out, N);

    cudaMemcpy(h_out, d_out, bytes, cudaMemcpyDeviceToHost);
    cudaFree(d_in);
    cudaFree(d_out);
}

---test-cases---
- name: "Uniform values"
  input: "32\nfill:3.0"
  expected: "fill:1024:3.0"
- name: "Zero matrix"
  input: "32\nfill:0.0"
  expected: "fill:1024:0.0"
- name: "Correctness"
  input: "verify 32"
  expected: "1"

---solution---
__global__ void transpose(const float* in, float* out, int N) {
    __shared__ float smem[TILE][TILE + 1];

    int col_in  = blockIdx.x * TILE + threadIdx.x;
    int row_in  = blockIdx.y * TILE + threadIdx.y;

    if (col_in < N && row_in < N)
        smem[threadIdx.y][threadIdx.x] = in[row_in * N + col_in];

    __syncthreads();

    int col_out = blockIdx.y * TILE + threadIdx.x;
    int row_out = blockIdx.x * TILE + threadIdx.y;

    if (col_out < N && row_out < N)
        out[row_out * N + col_out] = smem[threadIdx.x][threadIdx.y];
}

---harness---
#include <cstdio>
#include <cstring>
#include <cmath>
#include <vector>

int main() {
    char first[64];
    scanf("%s", first);

    if (strcmp(first, "verify") == 0) {
        int N; scanf("%d", &N);
        std::vector<float> hIn(N*N), hOut(N*N), ref(N*N);
        for (int i = 0; i < N; i++)
            for (int j = 0; j < N; j++)
                hIn[i*N+j] = (float)(i*N+j);
        for (int i = 0; i < N; i++)
            for (int j = 0; j < N; j++)
                ref[j*N+i] = hIn[i*N+j];
        solve(hIn.data(), hOut.data(), N);
        bool ok = true;
        for (int i = 0; i < N*N && ok; i++)
            if (fabsf(hOut[i] - ref[i]) > 0.5f) ok = false;
        printf("%d\n", ok ? 1 : 0);
    } else {
        int N = atoi(first);
        char tok[64];
        scanf("%s", tok);
        std::vector<float> hIn(N*N), hOut(N*N);
        if (strncmp(tok, "fill:", 5) == 0) {
            float v = atof(tok + 5);
            for (int i = 0; i < N*N; i++) hIn[i] = v;
        } else {
            hIn[0] = atof(tok);
            for (int i = 1; i < N*N; i++) scanf("%f", &hIn[i]);
        }
        solve(hIn.data(), hOut.data(), N);
        for (int i = 0; i < N*N; i++) {
            if (i) printf(" ");
            printf("%.1f", hOut[i]);
        }
        printf("\n");
    }
    return 0;
}

---editorial---
## Approach

The key problem with a naïve transpose (`out[col * N + row] = in[row * N + col]`) is that either reads or writes will be uncoalesced. If we read rows (coalesced) and write columns (uncoalesced), we get one global memory transaction per thread instead of one per warp — up to 32× worse.

The shared memory solution stages through SRAM:
1. Load `in[row][col]` into `smem[ty][tx]` — reads are coalesced (consecutive threads read consecutive addresses)
2. `__syncthreads()` — wait for the full tile
3. Write `smem[tx][ty]` to `out[blockIdx.x*TILE+tx][blockIdx.y*TILE+ty]` — writes are coalesced (consecutive threads write consecutive addresses in the transposed output)

The `+1` padding on shared memory (`float smem[TILE][TILE+1]`) shifts each row by one element, ensuring that threads in a warp access different banks when reading the transposed tile, eliminating shared memory bank conflicts.

## Complexity

- **Memory transactions**: O(N²/warpSize) — same as a simple copy, optimal
- **Shared memory**: TILE × (TILE+1) × 4 bytes per block (e.g., 4 KB for TILE=32)

## Key Insight

Without the `+1` padding, reading `smem[tx][ty]` accesses elements with stride TILE in memory. For 32-element warps and 32-bank shared memory, all 32 threads in a warp access the same bank — a 32-way bank conflict. The padding breaks the alignment.

## Common Mistakes

- **Forgetting the `+1` padding**: The transpose runs but is up to 16× slower due to bank conflicts.
- **Wrong output index**: The output block position swaps x and y: `blockIdx.x*TILE+tx` for the transposed row, `blockIdx.y*TILE+ty` for the transposed column.
- **Omitting `__syncthreads()`**: Without it, threads that write to smem fast may be overwritten before slow threads read.
---end---
