---
slug: reduce-sum
title: Parallel Reduction Sum
difficulty: hard
track: cuda-core
tags:
  - reduction
  - shared-memory
  - warp-primitives
status: published
author: community
xp: 400
runtime: cuda
cpp_standard: "17"
cuda_min_version: "13.0"
compute_min_cap: "sm_120"
---

## Background

**Parallel reduction** is a fundamental pattern in GPU computing: combining N elements into a single result (sum, max, min) using O(log N) parallel steps. A naïve single-threaded sum runs in O(N); a well-implemented GPU reduction runs in O(N/P) where P is the number of parallel processors.

The classic approach uses **shared memory**: each block loads a contiguous chunk of the input, then performs a tree-reduction within the block, halving the active thread count at each step. After the block writes its partial sum to a global array, a second kernel pass (or a single-block final reduction) collapses all partial sums.

For maximum throughput on modern NVIDIA hardware, avoid shared memory entirely within a warp and instead use **warp-shuffle intrinsics** (`__shfl_down_sync`). These intrinsics allow threads within a warp to directly exchange register values without going through shared memory, saving both latency and bandwidth.

## What to Implement

Implement `reduceSum` that computes the sum of an array of `n` floats and writes the result to `*out`. The expected approach:

1. Each thread loads one element and accumulates into a local register
2. Use `__shfl_down_sync(0xffffffff, val, offset)` for the warp-level reduction (offset = 16, 8, 4, 2, 1)
3. Lane 0 of each warp writes the warp partial sum to shared memory
4. The first warp reduces across all per-warp partial sums
5. Thread 0 of the block writes the block partial sum to `partials[blockIdx.x]`
6. The host (already provided) calls a second pass to sum the block partials

## Constraints

- `n` is a power of 2, `1 ≤ n ≤ 2^24`
- Use `blockDim.x = 256` (8 warps of 32 threads each)
- The result must match a sequential CPU sum within floating-point rounding tolerance (1e-3 relative error)
- Do not modify `solve()`

## Example

**Input:** `[1.0, 2.0, 3.0, 4.0]` (n=4)

**Output:** `10.0`

---starter-code---
#include <cuda_runtime.h>
#include <cstdio>

#define BLOCK_SIZE 256
#define WARP_SIZE  32

// TODO: Each block reduces its slice to a single partial sum written to partials[blockIdx.x].
// Use __shfl_down_sync for warp-level reduction.
__global__ void reduceBlock(const float* in, float* partials, int n) {
    // Your code here
}

// Second-pass: sums the partials array (called by solve with gridSize=1).
__global__ void reducePartials(float* partials, float* out, int count) {
    // Your code here
}

// Do NOT modify below this line.
void solve(const float* h_in, float* h_out, int n) {
    float *d_in, *d_partials, *d_out;
    int gridSize = (n + BLOCK_SIZE - 1) / BLOCK_SIZE;

    cudaMalloc(&d_in,       n        * sizeof(float));
    cudaMalloc(&d_partials, gridSize * sizeof(float));
    cudaMalloc(&d_out,      sizeof(float));

    cudaMemcpy(d_in, h_in, n * sizeof(float), cudaMemcpyHostToDevice);

    reduceBlock<<<gridSize, BLOCK_SIZE>>>(d_in, d_partials, n);
    reducePartials<<<1, BLOCK_SIZE>>>(d_partials, d_out, gridSize);

    cudaMemcpy(h_out, d_out, sizeof(float), cudaMemcpyDeviceToHost);
    cudaFree(d_in);
    cudaFree(d_partials);
    cudaFree(d_out);
}

---test-cases---
- name: "Small sum"
  input: "4\n1.0 2.0 3.0 4.0"
  expected: "10.0"
- name: "Zero sum"
  input: "8\n1.0 -1.0 2.0 -2.0 3.0 -3.0 4.0 -4.0"
  expected: "0.0"
- name: "Large uniform"
  input: "256\nfill:1.0"
  expected: "256.0"

---solution---
__global__ void reduceBlock(const float* in, float* partials, int n) {
    int idx = blockIdx.x * blockDim.x + threadIdx.x;
    float val = (idx < n) ? in[idx] : 0.0f;

    // Warp-level reduction using shuffle
    for (int offset = WARP_SIZE / 2; offset > 0; offset >>= 1)
        val += __shfl_down_sync(0xffffffff, val, offset);

    // Write per-warp result to shared memory
    __shared__ float warp_sums[BLOCK_SIZE / WARP_SIZE];
    int lane   = threadIdx.x % WARP_SIZE;
    int warp_id = threadIdx.x / WARP_SIZE;
    if (lane == 0) warp_sums[warp_id] = val;
    __syncthreads();

    // First warp reduces the per-warp sums
    if (warp_id == 0) {
        val = (lane < (BLOCK_SIZE / WARP_SIZE)) ? warp_sums[lane] : 0.0f;
        for (int offset = WARP_SIZE / 2; offset > 0; offset >>= 1)
            val += __shfl_down_sync(0xffffffff, val, offset);
        if (lane == 0) partials[blockIdx.x] = val;
    }
}

__global__ void reducePartials(float* partials, float* out, int count) {
    float val = (threadIdx.x < count) ? partials[threadIdx.x] : 0.0f;
    for (int offset = WARP_SIZE / 2; offset > 0; offset >>= 1)
        val += __shfl_down_sync(0xffffffff, val, offset);
    __shared__ float warp_sums[BLOCK_SIZE / WARP_SIZE];
    int lane = threadIdx.x % WARP_SIZE;
    int warp_id = threadIdx.x / WARP_SIZE;
    if (lane == 0) warp_sums[warp_id] = val;
    __syncthreads();
    if (warp_id == 0) {
        val = (lane < (BLOCK_SIZE / WARP_SIZE)) ? warp_sums[lane] : 0.0f;
        for (int offset = WARP_SIZE / 2; offset > 0; offset >>= 1)
            val += __shfl_down_sync(0xffffffff, val, offset);
        if (lane == 0) *out = val;
    }
}

---harness---
#include <cstdio>
#include <cstring>
#include <vector>

int main() {
    int n;
    scanf("%d", &n);
    std::vector<float> h_in(n);
    char tok[64];
    scanf("%s", tok);
    if (strncmp(tok, "fill:", 5) == 0) {
        float v = atof(tok + 5);
        for (int i = 0; i < n; i++) h_in[i] = v;
    } else {
        h_in[0] = atof(tok);
        for (int i = 1; i < n; i++) scanf("%f", &h_in[i]);
    }
    float result = 0.0f;
    solve(h_in.data(), &result, n);
    printf("%.4f\n", result);
    return 0;
}

---editorial---
## Approach

Parallel reduction folds N inputs into one result using two kernel passes. Each block independently reduces its chunk to one partial sum, then a second pass reduces the array of partial sums.

**Within a block, use warp-shuffle intrinsics:**

```cpp
// Warp-level reduction (no shared memory needed within a warp)
for (int offset = WARP_SIZE / 2; offset > 0; offset >>= 1)
    val += __shfl_down_sync(0xffffffff, val, offset);
```

After the warp reduction, lane 0 of each warp holds the warp partial sum. Write these to shared memory, then have the first warp reduce across all warp sums.

**Two-pass structure:**
- `reduceBlock`: each block reduces its slice to `partials[blockIdx.x]`
- `reducePartials`: a single block (gridSize=1) reduces the partials array

## Complexity

- **Time**: O(N/P) where P is total threads (two passes: O(N/256) + O(gridSize/256))
- **Shared memory**: BLOCK_SIZE/WARP_SIZE floats = 8 floats per block

## Key Insight

`__shfl_down_sync(mask, val, offset)` exchanges register values between threads in the same warp without touching shared memory. This is faster than a shared-memory tree reduction because it avoids memory latency and bank conflicts. The mask `0xffffffff` means all 32 lanes participate.

## Common Mistakes

- **Out-of-bounds load**: Use `(idx < n) ? in[idx] : 0.0f` to handle arrays whose size isn't a multiple of BLOCK_SIZE.
- **Wrong warp count in shared memory**: The shared array needs `BLOCK_SIZE / WARP_SIZE` entries (= 8 for BLOCK_SIZE=256, WARP_SIZE=32), not WARP_SIZE entries.
- **Missing sync before first-warp reduction**: Thread divergence means the warp partial sums are written to shared memory by one thread per warp. `__syncthreads()` ensures all writes complete before the first warp reads them.
---end---
